---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "zeus_port Resource - zeus"
subcategory: ""
description: |-
  Zeus port forwarding rule
---

# zeus_port (Resource)

Zeus port forwarding rule

## Example Usage

```terraform
provider "zeus" {
  endpoint = "http://localhost:8080"
  token    = "changeme"
}

resource "zeus_assign" "vm" {
  region = ["us-east-1"]
  host   = "node-1"
  key    = "vm-123"
  type   = "vm"
}

resource "zeus_port" "ssh" {
  assign_id   = zeus_assign.vm.id
  scope_host  = "node-1"
  target_port = 22
  service     = "ssh"
}

data "zeus_port" "by_id" {
  id = zeus_port.ssh.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `assign_id` (String) Assign ID backing the port rule
- `service` (String) Service name
- `target_port` (Number) Target service port

### Optional

- `scope_host` (String) Optional create-time scope host sent as X-Portd-Host. This value is not reconstructed during read or import, so imported resources typically omit it.

### Read-Only

- `created_at` (String)
- `host` (String) Observed Zeus host for the port rule
- `id` (String) The ID of this resource.
- `port` (Number) Allocated external port

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env bash
terraform import zeus_port.ssh "port-id"
```
